# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'pruner' do
  # self.rubyforge_name = 'prunerx' # if different than 'pruner'
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=ruby
